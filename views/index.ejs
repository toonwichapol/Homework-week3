<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Form Add Student</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="styles.css">
</head>

<body>

    <form class="profile-detail" action="/students" method="post">

        <h3>รายชื่อนักเรียน</h3>

        <table class="table-border">
            <tr>
                <th>ID</th>
                <th>ชื่อ-สกุล</th>
                <th>อายุ</th>
                <th>เบอร์โทร</th>
                <th>Email</th>
            </tr>
            <% users.forEach(user=> { %>
                <tr>
                    <td>
                        <%= user.id %>
                    </td>
                    <td>
                        <%= user.name %>
                    </td>
                    <td>
                        <%= user.age %>
                    </td>
                    <td>
                        <%= user.phone %>
                    </td>
                    <td>
                        <%= user.email %>
                    </td>
                </tr>
                <% }); %>
        </table>

        <hr>

        <div class="row">

            <div class="col-md-6 col-sm-6 mt-3 form-group">

                <label for="Name"><b>ชื่อ-สกุล:</b></label>
                <input type="text" name="Name" id="Name" class="form-control" placeholder="กรุณาระบุชื่อ-สกุล">

            </div>

            <div class="col-md-6 col-sm-6 mt-3 form-group">

                <label for="Age"><b>อายุ:</b></label>
                <input type="number" name="Age" id="Age" class="form-control" value="0">

            </div>

            <div class="col-md-6 col-sm-6 mt-3 form-group">

                <label for="Tel"><b>เบอร์โทร:</b></label>
                <input type="text" name="Tel" id="Tel" class="form-control" placeholder="กรุณาระบุเบอร์โทรศัพท์">

            </div>

            <div class="col-md-6 col-sm-6 mt-3 form-group">

                <label for="Email"><b>Email:</b></label>
                <input type="email" name="Email" id="Email" class="form-control" placeholder="กรุณาระบุ Email">

            </div>

        </div>

        <div class="text-center mt-3">

            <button type="submit" class="btn btn-primary">เพิ่ม</button>

        </div>


    </form>

</body>

</html>